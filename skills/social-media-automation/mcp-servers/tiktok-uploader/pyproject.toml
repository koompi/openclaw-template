[project]
name = "tiktok-uploader"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "tiktokautouploader",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
